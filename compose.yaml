# UI development preview only. Run the service and aria2 on the host for RPC tests.
services:
  web:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.dev
    ports:
      - "127.0.0.1:5173:5173"
    init: true
